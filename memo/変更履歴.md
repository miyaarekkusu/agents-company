# 変更履歴メモ

このプロジェクト（AIエージェントお仕事ゲーム）でこれまでに行った作業の記録です。時系列でまとめています。
コミット単位の詳細は `git log` を参照してください。ここでは「何を・なぜ」やったかを中心に記載します。

## 2026-07-23: リポジトリ整理とAGENTS.md作成

- `agents-company`（GitHub上の本体リポジトリ）が `ai-company` フォルダの中に入れ子でクローンされてしまっていた問題を解消。空のプレースホルダーだった外側の `ai-company` リポジトリを削除し、`ai-company` 自体が `agents-company` の本物のクローンになるように統合した。
- `AGENTS.md` を新規作成。ゲームの企画意図（社長がスタッフAIエージェントを雇用・見守るシミュレーションゲーム）、ゲームシステム、技術スタック（React / FastAPI / Docker / PostgreSQL）をまとめた。
- リモートへのpush手順を確立: `git add .` → `git commit -m "..."` → `git checkout -b <ブランチ名>` → `git push origin <ブランチ名>`。以降この流れで統一。

## 2026-07-23: frontend / backend-core の環境構築

- `AGENTS.md` にディレクトリ構成（`frontend/` と `backend-core/` を分離するモノレポ構成）を明記した上で、別々のAIエージェントを並行して割り当てて構築。
- **frontend**: Vite + React + TypeScript。公式の `create-vite` 最新テンプレートを参照し、React 19.2.8 / Vite 8.1.5 / TypeScript 6.0.2（`typescript@latest`は7系だが、公式テンプレートがまだ6系採用のためそちらに合わせた）で構築。`npm install` / `npm run build` / `npm run dev` で動作確認済み。
- **backend-core**: FastAPI。Python 3.14、fastapi 0.139.2 / uvicorn 0.51.0 / sqlalchemy 2.0.51 / asyncpg 0.31.0 / pydantic-settings 2.14.2 / alembic 1.18.5（すべてPyPIで最新版を確認して採用）。`GET /health` を用意し、venv・Docker両方で動作確認済み。
- `docker-compose.yml` を作成し、`db`（PostgreSQL 18）/ `backend-core` / `frontend` の3サービスを結線。PostgreSQL 18でDockerイメージのデータ格納先仕様が変更されていた（`/var/lib/postgresql/data` ではなく `/var/lib/postgresql` をマウントする必要がある）ため対応。
- パッケージのバージョンはすべて一度公式ドキュメント/PyPI/npm registryで確認してから採用する方針で統一。

## 2026-07-23: agents/・skills/・memo/フォルダ追加、開発ルールの明文化

- 将来ゲーム内AIエージェントのペルソナ定義を置く `agents/` フォルダと、エージェントが使う「スキル」定義を置く `skills/` フォルダをプレースホルダーとして作成。
- `AGENTS.md` に開発ルールを追記:
  - 機能実装はタスクごとに別のAIエージェントを割り当てて作業する方針
  - 依頼されていない変更を勝手に行わない、担当範囲外に触れない方針
- `memo/` フォルダを作成し、`初期設定.md`・`バックエンド確認方法.md`・`git-push手順.md` を用意。
- コミットメッセージとコード内コメントを日本語に統一する方針を確認。既存の英語コミットメッセージ・英語コメントを日本語に修正し、force-pushで履歴を更新した。

## 2026-07-24: エージェント・オーケストレーション方式の検討（LangGraph採用）

- CrewAI・LangGraph・自前実装の3つを比較検討。
  - CrewAIは`Crew.kickoff()`がバッチ実行寄りで、会議室・作業室の進行をリアルタイムにストリーミング表示したい要件と相性が悪いため見送り。
  - 自前実装は学習にはなるが、状態管理・ストリーミング・永続化などの「配管」を一から作る必要があり負担が大きい。
  - LangGraphはState/Node/Edgeを自分で明示的に設計する必要があり学習効果を損なわず、かつストリーミング・永続化・human-in-the-loopが標準機能として揃っている点、LangChain本体なしで外部APIを直接ノード内から呼べる点が決め手となり採用を決定。

## 2026-07-24: LangGraphによる最小構成（歩行スケルトン）の実装

- 最初の外部LLM APIとして、実際にAPIキーを持っているDeepSeek APIを採用（OpenAI互換API。`openai` SDKで`base_url`を差し替えて利用）。`deepseek-chat` / `deepseek-reasoner` が公式に非推奨化されるタイミングだったため、後継の `deepseek-v4-pro` を使用。
- `backend-core/pyproject.toml` に `langgraph>=1.2.9` / `openai>=2.47.0` を追加（PyPIで最新版を確認済み）。
- `app/services/llm.py`: DeepSeek呼び出しの薄いラッパーを追加。
- `app/services/graph.py`: 「社長のお題 → 1エージェントがLLM呼び出し → 結果」という一本道のLangGraphグラフ（State/Node/Edge）を追加。DB永続化・複数エージェント・ストリーミングはまだ実装せず、最小構成にとどめた。
- `app/schemas/task.py` / `app/api/tasks.py`: `POST /api/tasks` エンドポイントを追加し、`main.py` にルーター登録。
- 動作確認: venv・Docker Compose（`docker compose build backend-core` で新依存関係を反映）の両方で `GET /health`（デグレなし）と `POST /api/tasks`（日本語のお題を送りDeepSeekから日本語の応答が返る）を確認済み。
- `.env`（実際のAPIキーを含む・gitignore対象）と `.env.example`（コミットされる安全なテンプレート）の役割分担を確認。`DATABASE_URL`のデフォルト値はローカル専用の汎用認証情報のため、`.env.example`に残しても問題ないことを確認済み。

## 2026-07-24: ターミナルからの動作確認手段の追加と.env読み込みの不具合修正

- `curl`でのJSON手打ちが面倒という指摘を受け、`backend-core/scripts/chat.py`を追加。サーバー起動やcurlなしで、LangGraphのグラフを直接呼び出してターミナルで対話しながら動作確認できる簡易CLI。
- `scripts/chat.py`を`backend-core`以外のディレクトリから実行すると `DEEPSEEK_API_KEY` が読み込まれずエラーになる不具合を発見。原因は `app/core/config.py` の `.env` 読み込みが実行時のカレントディレクトリに依存する相対パス（`env_file=".env"`）になっていたこと。`config.py`からの絶対パス（`backend-core/.env`）を明示的に指定する形に修正し、どのディレクトリから実行しても`.env`を読み込めるようにした。
- 動作確認中に、DeepSeekのモデルが自己紹介で「Anthropicが開発したClaudeです」のように別のAIを名乗る現象を確認。通信・実装側のバグではなく、モデル側の自己認識が学習データの影響で混乱する既知の現象。エージェントに正しい自己認識を持たせるには、systemプロンプトで明示的にアイデンティティを伝える必要があることが分かった。今後エージェントの人格・システムプロンプトを設計する際の留意点として記録。

## 2026-07-25: 最初のキャラクターエージェント「アイデア出しエージェント（ひらめきポン太）」の実装

- これまでの`graph.py`の唯一のノードは、キャラクター性のない無個性なDeepSeek呼び出しだった。今回、ゲーム内で最初の実在するスタッフキャラクターとして「アイデア出しエージェント（ひらめきポン太）」を実装した。
- AGENTS.mdの「開発ワークフロー（タスクとエージェントの分割）」ルールに従い、ペルソナ作成担当とバックエンド実装担当の2つのAIエージェントを並行して割り当てて作業した。
  - ペルソナ担当: `agents/idea_agent.md`を新規作成。キャラクター設定（名前・性格・口調）とシステムプロンプトを、パース可能な固定フォーマット（`## システムプロンプト`見出し直後に`text`コードブロック）で記述。
  - バックエンド担当: `app/services/llm.py`の`call_deepseek`に`system_prompt`引数を追加、`app/services/persona.py`を新規作成して`agents/`配下のMarkdownからシステムプロンプトを抽出するローダーを実装、`app/services/graph.py`のノードをアイデア出しエージェント専用（`idea_agent_node`）に更新。
- `app/api/tasks.py`・`app/schemas/task.py`（APIの入出力形式）は変更なし。既存のクライアント（`scripts/chat.py`・curl経由の疎通確認）はそのまま使える。
- `scripts/chat.py`で動作確認済み。お題を送るとひらめきポン太のキャラクター（「〜っしょ！」「〜じゃん！」等の口調）で複数のアイデアを番号付きで返すことを確認。自己紹介をさせても正しく「ひらめきポン太（DeepSeekベース）」と名乗り、Claude等の誤認識は発生しなかった。

## 2026-07-25: お題を出す際にエージェントを選択できるように対応（DBはまだ使わない）

- 今後複数のエージェントを追加していく前提で、社長がお題を出す際に「どのエージェントに依頼するか」を選べるようにした。エージェントがまだ1体（ひらめきポン太）しかいないため、DBは使わず`backend-core/app/services/agents_registry.py`にPythonの静的な辞書（`agent_id` → 名前・ペルソナファイル名）で登録する方式にした。DBスキーマ・API設計は別タスクとして後日着手する（AGENTS.mdのロードマップに記載）。
- 2つのAIエージェントを並行して割り当てて作業した（共通で使う`agents_registry.py`は先に自分で作成し、それを土台に依頼した）。
  - 担当1: `GET /api/agents`（登録済みエージェント一覧を返すAPI）を新規実装。`app/schemas/agent.py`・`app/api/agents.py`を新規作成し、`main.py`にルーター登録。
  - 担当2: `POST /api/tasks`に`agent_id`（必須）を追加し、指定されたエージェントのペルソナで応答するよう`app/services/graph.py`を更新。未登録の`agent_id`が指定された場合は`app/api/tasks.py`で404を返すようにした。`scripts/chat.py`にも、対話開始時に登録済みエージェント一覧から選ばせるUIを追加。
- 動作確認: `GET /api/agents`・`POST /api/tasks`（正常系・未登録agent_idでの404）・`scripts/chat.py`（エージェント選択→お題→応答）をすべて確認済み。既存の`GET /health`にもデグレなし。
- 2体目以降のエージェントを追加する場合は、`agents/`に新しいペルソナファイル（`agents/idea_agent.md`と同じフォーマット）を作り、`app/services/agents_registry.py`の`AGENT_REGISTRY`に1行追加するだけで済む構成にしてある。

## 2026-07-27: DB実装（SQLAlchemyモデル・Alembicマイグレーション・seed・エージェント登録のDB化）

`DATABASE.md`の今後の実装ステップ1〜4に着手し、完了した。詳細な手順・トラブルシューティングは`memo/DB利用方法.md`にまとめた。

- `backend-core/app/models/`に`DATABASE.md`の全13テーブルに対応するSQLAlchemyモデルを実装（自分で担当。DBスキーマの正確性を優先し、複数エージェントへの分割はここでは行わなかった）。
- Alembicの初期マイグレーションを生成・適用するエージェントを起動 → 途中で停止されたため、残り作業（`alembic revision --autogenerate`の実行・適用確認）は自分で引き継いで完了。全13テーブル＋`alembic_version`がPostgreSQLに作成されることを確認。
- `roles`（7役職）・`ai_models`（3件、deepseek-v4-proのみ実呼び出し可・claude-opus/haikuはマスタ登録のみ）・`skills`（3件、仮データ）・`agents`（ひらめきポン太1体、`system_prompt`は`agents/idea_agent.md`から動的読み込み）を投入する`scripts/seed_db.py`を作成するエージェントを起動、完了・動作確認済み（再実行安全性も確認）。
- `app/services/agents_registry.py`をPythonの静的な辞書からDB参照に切り替えるエージェントを起動、完了・動作確認済み。これにともない**`agent_id`の型が文字列からDBの`agents.id`（数値）に変更**。影響範囲（`schemas/task.py`・`schemas/agent.py`・`api/tasks.py`・`api/agents.py`・`services/graph.py`・`scripts/chat.py`）も同エージェントが対応。`app/services/persona.py`は、以降`scripts/seed_db.py`等の初期データ投入専用となり、リクエスト処理では使わなくなった。
- venv・Docker Compose（`docker compose build backend-core`で再ビルド）の両方で、`GET /health`（デグレなし）・`GET /api/agents`（数値の`agent_id`）・`POST /api/tasks`（正常系・存在しない`agent_id`での404）・`scripts/chat.py`（エージェント選択→お題→応答）を確認済み。
- `AGENTS.md`・`DATABASE.md`のロードマップ・実装ステップの該当項目を完了済みに更新。

## 2026-07-27: ゲームの一連の流れ（雇用・お題・会議）をCLIで確認できるように実装

社長から「実際のゲームフロー（雇用→お題→会議）を、フロントエンドを待たずにターミナルの番号入力で確認したい」という依頼を受け、Plan Modeで計画を作成・承認を得た上で実装した。あわせて「ひらめきポン太」の口調（「〜っしょ！」等）を落ち着いた話し方に変更。

- **口調変更**: `agents/idea_agent.md`のシステムプロンプトから軽い語尾・タメ口指示を削除し、落ち着いた丁寧な話し方に変更。DBの`agents.system_prompt`は`seed_db.py`実行時にファイルからコピーされる仕様のため、`seed_agents()`を「既存行はUPDATEする」方式に変更した上で`python scripts/seed_db.py`を再実行し、DB側にも反映。
- **プロバイダ振り分け**: `app/services/llm.py`に`call_llm(agent, prompt)`を追加。`agent.ai_model.provider`が`"deepseek"`なら実際に呼び、それ以外（`claude`等、`ai_models`にマスタ登録のみされているモデル）は`NotImplementedError`を送出するようにし、`app/services/graph.py`もこちら経由に変更。
- 2つのAIエージェントを並行して割り当てて実装した。
  - 社長室担当: `app/services/hiring.py`（役職・スキル・AIモデル選択によるエージェント雇用。上流工程系の役職はAIモデル選択をハード制限）、`app/services/work.py`（お題実行→`Task`/`Artifact`作成、成果物一覧取得）を新規実装。
  - 会議室担当: `app/services/meeting_graph.py`（参加者が提案→リーダーが集約するLangGraphフロー）、`app/services/meetings.py`（`Meeting`/`MeetingParticipant`/`MeetingProposal`/`MeetingReport`のDB永続化、「会議室は1つ・同時進行1件まで」のアプリ側チェック）を新規実装。
- 上記2つの完了後、自分で統合CLI `backend-core/scripts/game_cli.py` を実装。「どの部屋にいますか？」→社長室/会議室/作業室を選び、部屋ごとに異なる操作ができる二階層メニュー。社長室でのお題実行結果はその場では見せず、作業室で確認する動線にした（`memo/game_cli使い方.md`に詳細）。
- 実際に「バックエンドエージェント」を雇用 → お題を出して実行 → 作業室で結果確認 → ひらめきポン太とバックエンドエージェントで会議（リーダー・参加者選択）→提案とレポート表示、という一連の流れを最後まで動作確認済み。
- `AGENTS.md`・`DATABASE.md`のロードマップ・実装ステップの該当項目を完了済みに更新。

## 2026-07-27: お題の複数人担当・会議室のお題を社長室登録済みのものに限定（修正）

上記CLI実装のフィードバックとして、2点の修正依頼を受け対応した。

- **お題の複数人担当**: `app/services/work.py`を、1つのお題(`Task`)に対して1体のエージェントしか割り当てられなかった実装から、`create_task`（お題登録）と`run_task_for_agent`（1体ぶんの実行）に分割し、`run_task`が複数の`agent_id`を受け取って各エージェント個別に`Artifact`を作成するように変更。1体が失敗（未対応プロバイダ等）しても、他のエージェントの結果は失わない（`TaskAssignmentOutcome`で成功/失敗を個別に返す）。
- **会議室のお題を社長室登録済みのものに限定**: 従来は会議室でその場に議題テキストを入力し、`run_meeting`が新規に`Task`を作成していたが、これを`work.list_tasks`で**社長室で登録済みのお題一覧から選ぶ**方式に変更。`meetings.run_meeting`は`task_text`ではなく`task_id`を受け取り、既存の`Task`を参照するのみで新規作成しない。
- `scripts/game_cli.py`の社長室「お題を出す」（複数人選択に対応）・会議室「会議を開く」（お題を一覧から選択）を合わせて更新し、複数人担当→作業室での各人の成果物確認、および社長室で登録したお題を会議室で選んで会議を開く、という流れを実際に動作確認済み。
- 追加要望として、社長室に「登録エージェント一覧を見る」を追加。`app/services/hiring.py`に`list_agent_details`（役職名・使用AIモデル名・スキル名をまとめて返す）を実装し、`game_cli.py`から性格・役職・使用AIモデル・スキルを一覧確認できるようにした。

## 2026-07-27: エージェントの編集・削除機能を追加

一覧確認機能を追加した際、ひらめきポン太にスキルが1つも設定されていないことに気づいたのをきっかけに、「登録済みのエージェントを編集・削除できるようにしてほしい」という要望を受け対応した。

- `app/services/hiring.py`に`get_agent_detail`（1体分の詳細取得）・`update_agent`（名前/性格/役職/AIモデル/システムプロンプト/スキルを個別に更新。スキルは指定した内容に洗い替え）・`delete_agent`（`agent_skills`を先に削除してから`agents`行を削除。既に`artifacts`/`meetings`等から参照されているエージェントを削除しようとすると外部キー制約違反になるため、`IntegrityError`を捕捉して分かりやすい`ValueError`に変換）を追加。
- `scripts/game_cli.py`の社長室に「4. エージェントを編集する」「5. エージェントを削除する」を追加。編集は名前・性格・役職・AIモデル・スキルを個別に選んで更新でき、名前/性格/役職/AIモデルを変えるとシステムプロンプトも自動的に作り直す。役職を変更した場合はAIモデルの選び直しも行う（ハード制限の対象が変わりうるため）。
- 動作確認: ひらめきポン太にスキル「アイデア発想」を編集で追加できることを確認。既に使用済みのエージェント（ひらめきポン太等）の削除を試みると「このエージェントは既にお題や会議で使われているため削除できません」と表示され削除されないこと、未使用の新規エージェントは正常に削除できることの両方を確認済み。
- 続けて「社長室でお題一覧も見れるようにしてほしい」という要望を受け、`scripts/game_cli.py`の社長室に「6. お題一覧を見る」を追加。既存の`work.list_tasks`をそのまま利用し、登録済みお題をタイトル・`task_id`・進行状況・登録日時付きで新しい順に一覧表示するようにした（動作確認済み）。

## 2026-07-27: 途中キャンセル対応とメニュー並び順の調整

「エージェントやお題作成時に誤って押したとき、キャンセルできるように」「お題一覧をお題作成の下にしてほしい」という要望を受け対応した。

- `scripts/game_cli.py`の`_prompt_choice`（役職・AIモデル・エージェント・お題・リーダー選択などで共通利用する番号選択の入力受付関数）を、`0`を入力すると`None`を返してキャンセル扱いにできるように変更。呼び出し側（`_select_role`/`_select_ai_model`/`_choose_agent`、および`open_meeting_flow`のお題・リーダー選択、`view_work_flow`の成果物選択）はこれを受けて「キャンセルしました。」と表示し処理を中断する。
- カンマ区切りの複数選択（お題を出す際のエージェント選択、会議室の参加者選択）にも同様に`0`単体入力でキャンセルできる分岐を追加。
- `edit_agent_flow`の役職変更（選択肢3）でも、役職選択をキャンセルした場合に変更せず編集メニューへ戻るよう修正（元は`_select_role`がNoneを返す可能性を考慮していなかった）。
- 社長室のメニュー順を「1.エージェント作成 → 2.お題を出す → 3.お題一覧を見る → 4.登録エージェント一覧 → 5.編集 → 6.削除」に並び替え、お題関連（作成・一覧）とエージェント管理（一覧・編集・削除）がそれぞれまとまるようにした。
- 動作確認: エージェント作成時の役職選択でのキャンセル、お題を出す際のエージェント選択でのキャンセル、会議室のお題選択でのキャンセル、編集メニューの役職変更でのキャンセル、および新しいメニュー順序が意図通り動くことを確認済み。
- 「参照や番号を選択した後、time.sleep(1)を入れてほしい」という要望を受け、`scripts/game_cli.py`の`_prompt_choice`（番号選択が確定した直後）と、一覧参照系の各処理（登録エージェント一覧・お題一覧・作業内容確認・会議レポート表示・お題実行結果表示・編集メニューでの現在値表示・スキル/参加者などのカンマ区切り複数選択）の末尾に`time.sleep(1)`を追加し、選択・表示内容を読む間を置くようにした。

## 2026-07-27: お題の状態遷移（会議要否・レポート承認ゲート）と1お題1成果物の協力作業フローを実装

「お題を作成したとき会議室での話し合いが必要かどうかを選択できるように」「必要な場合はレポート作成・社長の承認まで作業が始まらないように」「1つのお題に対して成果物は1つ、複数人でタスク役割分担して協力・リーダーも置く」という要望を受け、Plan Modeで計画を作成・承認を得た上で実装した。前回実装した「1エージェント1成果物」方式（複数エージェントがそれぞれ個別に成果物を作る）はこの設計に合わないため置き換えた。

- **DBスキーマ変更**: `tasks.requires_meeting`（会議要否、bool）・`meeting_reports.approved_at`（社長の承認日時、nullable）を追加。Alembicマイグレーション（`aec8aa81e7a1_task_requires_meeting_and_report_.py`）を生成・適用。既存行があるため`requires_meeting`は`server_default=false`を指定して追加した。
- **お題の状態遷移**: `tasks.status`を`awaiting_meeting`（会議待ち）→`awaiting_approval`（レポート承認待ち）→`ready_for_work`（作業可能）→`completed`（完了）の4値で管理するように変更（`requires_meeting=False`なら`awaiting_meeting`/`awaiting_approval`を経由せず`ready_for_work`から開始）。
- `app/services/work.py`: `create_task`が`requires_meeting`を受け取るように変更。**旧`run_task`/`run_task_for_agent`/`TaskAssignmentOutcome`（1エージェント1成果物方式）は削除**し、代わりに`assign_work`（担当者複数＋リーダーを受け取り、単一の`Artifact`を作成）・`list_ready_for_work_tasks`・`list_awaiting_meeting_tasks`を新設。
- 新規`app/services/work_graph.py`: `meeting_graph.py`と同型のLangGraphフロー。各workerが担当パートを作成→リーダーが1つの成果物に統合する（`WorkState`/`run_work_node`）。お題に会議レポートが紐づいていれば、その内容も踏まえて作業する。
- `app/services/meetings.py`: `run_meeting`に「`requires_meeting=True`かつ`status == awaiting_meeting`」のガードを追加。会議完了後の`task.status`を`"completed"`ではなく`"awaiting_approval"`に変更。新規`list_reports_pending_approval`・`approve_report`（承認して`ready_for_work`に進める）を追加。
- `scripts/game_cli.py`: 社長室のメニューを「1.雇用 2.お題を作成する 3.作業を割り当てる 4.レポートを確認して承認する 5.お題一覧 6.エージェント一覧 7.編集 8.削除」に再編。「お題を作成する」は会議要否を聞くだけ（担当者選択はしない）に変更し、新規に「作業を割り当てる」「レポートを確認して承認する」を追加。会議室のお題選択は`list_awaiting_meeting_tasks`（会議要・未実施のもの限定）に変更。お題一覧の表示にstatusの日本語ラベルを追加。
- 動作確認: ①会議不要のお題→即座に「作業を割り当てる」に出現→worker+リーダー選択→単一成果物作成→作業室で確認、の一連の流れ。②会議必要のお題→「作業を割り当てる」には出ない→会議室で話し合い→レポート作成→お題一覧で「レポート承認待ち」表示→社長室で承認→「作業可能」に変化→「作業を割り当てる」に出現→worker+リーダーで単一成果物作成、という一連の流れ。両方ともDBの状態（`tasks.status`・`artifacts`件数）とあわせて実際に確認済み。

## 2026-07-29: お題履歴のDB一括削除、お題の編集・削除機能、会議室のお題一覧表示

「お題履歴を一回全部消してほしい（DB整合性を保って）」「会議必要なお題一覧を会議室にしてほしい。社長室ではお題編集と削除を可能にする」という要望への対応。

- 開発用DBの`tasks`・`meetings`・`meeting_participants`・`meeting_proposals`・`meeting_reports`・`artifacts`・`email_threads`・`email_messages`を`TRUNCATE ... RESTART IDENTITY CASCADE`で一括削除（`agents`等のマスタデータは維持）。
- `app/services/work.py`: `update_task`（お題内容・会議要否の部分更新。完了済み/進行中のお題や、既に会議実施済みのお題の会議要否変更はガード）・`delete_task`（`meetings`系・`artifacts`・`email_threads`系を先に削除してから本体を削除。外部キー制約違反を回避）を新規追加。
- `scripts/game_cli.py`: 社長室に「お題を編集する」「お題を削除する」を追加（メニュー6・7番、既存のエージェント編集・削除は8・9・10番に繰り下げ）。会議室に「会議が必要なお題一覧を見る」を追加。
- 動作確認: お題の内容変更・会議要否のON/OFF切り替え（状態が連動して切り替わることを確認）・削除（一覧から消えることを確認）を一通り実施。

## 2026-07-29: 会議・作業の並行処理化、メール通知、エージェントの稼働状況管理

「並行処理ってできない？会議中では他の操作ができなくなる」「会議・作業が終わったときメールに通知が来るようにしてほしい」「会議中・作業中のエージェントはお題一覧でステータス表示、他のお題の作業はできないように」という要望を受け、Plan Modeで計画を作成・承認を得た上で実装した。

- **非ブロッキングI/O**: `scripts/game_cli.py`の全`input()`呼び出しを、別スレッドで実行する`_ainput()`に置き換えた（`time.sleep(1)`も`asyncio.sleep(1)`に変更）。同期的な`input()`のままだとイベントループ全体が止まり、バックグラウンドの会議・作業が一切進まなくなるため。
- **DBスキーマ変更**: `tasks.status`に`meeting_in_progress`・`work_in_progress`を追加（カラム変更は無く、取りうる値が増えただけ）。新規`work_sessions`/`work_session_participants`テーブル（`meetings`/`meeting_participants`と同型。作業の予約・担当者追跡用）を追加し、Alembicマイグレーション（`2d2ab090b447_work_sessions追加と進行中ステータス.py`）を生成・適用。
- **会議・作業を「予約→バックグラウンド実行」の2段階に分割**: `app/services/meetings.py`の`run_meeting`を`start_meeting`（予約。`Meeting`/`MeetingParticipant`作成、`task.status`を`meeting_in_progress`に、即座にコミットして返る）と`finish_meeting`（バックグラウンド。新しいセッションでLLM呼び出し→結果保存、失敗時は`awaiting_meeting`に戻す）に分割。`app/services/work.py`も同様に`assign_work`を`start_work`/`finish_work`に分割。`scripts/game_cli.py`は予約フェーズだけ`await`し、`asyncio.create_task`でバックグラウンド化してすぐメニューに戻る。CLI終了時（部屋選択で「0」）は、進行中のバックグラウンドタスクの完了を待ってから終了するようにした（そうしないと`asyncio.run`終了時にタスクがキャンセルされ、結果が失われるため）。
- 新規`app/services/availability.py`: 進行中（`in_progress`）の会議・作業に参加しているagent_idを返す`list_busy_agent_ids`と、あるお題を今誰が担当しているかを返す`get_in_progress_agent_names`。`start_meeting`/`start_work`はここでbusyな参加者を弾き、`scripts/game_cli.py`側の参加者・worker選択でも事前に選択肢から除外する（`_choose_available_agents`）。
- 新規`app/services/notifications.py`: 既存の`email_threads`/`email_messages`テーブル（`DATABASE.md`に定義済みだったが未実装）を使い、会議・作業の完了/失敗を社長に一方向で知らせる通知を実装（`notify`/`count_unread`/`list_notifications`/`mark_read`）。返信機能は対象外。
- `scripts/game_cli.py`に「4. メール」部屋を新設（未読件数をトップメニューに表示、「受信箱を見る」で内容確認・既読化）。お題一覧（`view_tasks_flow`）は`meeting_in_progress`/`work_in_progress`のお題に担当エージェント名を添えて表示するように変更。
- 動作確認: ①会議不要のお題で作業を割り当て→即座にメニューに戻る→CLI終了操作で完了を待ってから終了→DBで`work_sessions.status`/`tasks.status`/`artifacts`/`email_threads`が正しく更新されていることを確認。②メール部屋で通知を確認→既読化で未読件数が減ることを確認。③会議を開始した状態で、その参加者を別のお題の作業割り当てに使おうとすると選択肢から除外されることを確認。
- **手違いの記録**: 動作確認後のテストデータ削除作業で、CLIの「お題を削除する」を連続実行する際に一覧の番号がずれ、意図せず既存のお題（task_id=2、「いいデザインの自己紹介サイトを作成してくれ。」、レポート承認待ち状態）を誤って削除してしまった。会議・レポートの内容は復元不可（DBから完全削除のため）。ユーザーに要報告・要確認。
- 追加対応: 「会議中ならこの状態にしてほしい 会議室→会議室（使用中）。作業室も同じく」という要望を受け、部屋選択メニューの表示に稼働状況を反映した。`app/services/work.py`に`has_in_progress_work`（`work_sessions.status == "in_progress"`の有無を確認）を追加し、`meetings.has_in_progress_meeting`と合わせて`scripts/game_cli.py`の`main()`で毎回問い合わせ、「会議室（使用中）」「作業室（使用中）」のように表示を切り替える。会議室は同時1件までの制約があるため二値、作業室は複数件並行できるため「1件以上進行中か」で判定する（表示のみで入室制限はしない）。
- 追加対応: 「会議が終わったとき、レポート内容を受信箱のみで確認する（画面に出さなくていい）」という要望を受け、`meetings.py`の`finish_meeting`が作成する完了通知メールの本文に**会議レポートの全文**を含めるように変更（従来は「レポートが作成されました」という短い案内のみ）。`scripts/game_cli.py`の`approve_report_flow`からは、レポート内容を直接画面に表示する処理を削除し、「レポート内容はメールでご確認ください」と案内した上で承認可否だけを聞くようにした（レポート内容の確認経路をメールの受信箱のみに一本化）。
- 「メッセージ一つしかないのにメール(2)が出てくる」という報告を受け調査したが、`notify()`は会議・作業の完了/失敗それぞれで1回しか呼ばれておらず、通知作成箇所に重複は見当たらなかった。調査時点のDBの状態（`email_threads`の件数と`count_unread`の結果）は一致しており、再現できなかった。テスト中に複数の通知（会議・作業それぞれ1件ずつ等）が同時に未読で溜まっていたタイミングを見た可能性がある。再発する場合は、その時点でのメール一覧・件数を確認できるようにしてから報告してもらう。
- 追加の方針転換: 直前のラウンドで「レポート内容をメールに含め、承認画面では表示しない」に変更したが、「承認前にレポート内容を画面に出してほしい。メールは完了通知のみでよい。レポート完成時に画面に出す必要はない（バックグラウンドのままでよい）」という要望を受けて元に戻した。`meetings.py`の`finish_meeting`の通知メール本文を「レポートが作成されました」という短い案内のみに戻し、`scripts/game_cli.py`の`approve_report_flow`でレポート内容を画面表示する処理を復元した。

## 2026-07-29: 作業室でのリアルタイム表示（ストリーミング）を実装

ロードマップの「リアルタイムストリーミング配信」に着手。当初はフロントエンドへのSSE配信を想定していたが、要望のやり取りの中で「フロントエンドはまだ実装しないのでCLIで」「`scripts/chat.py`ではなくgame_cli.pyの作業室で、作業内容を確認する選択時に進行中の内容も見られるように」という形にスコープが固まり、Plan Modeで設計を作成・承認を得た上で、複数エージェント（バックエンド担当・CLI表示担当）に分けて実装した。

- `app/services/llm.py`: 既存`call_deepseek`/`call_llm`はそのまま残し、`stream_deepseek`/`stream_llm`（`AsyncIterator[str]`を返すストリーミング版）を新規追加。
- `app/services/work_graph.py`: `run_work_node`を`stream_llm`＋LangGraphの`get_stream_writer`（`from langgraph.config import get_stream_writer`）を使う形に変更し、各worker・リーダーの生成中に`agent_start`/`token`/`agent_end`のcustomイベントを流すようにした。`WorkState`・グラフ構造自体は変更なし。
- 新規`app/services/work_progress.py`: バックグラウンド実行中の生成途中の内容を、プロセス内メモリ（モジュール変数の辞書）で保持する進行状況ストア（`record_token`/`record_final`/`get_progress`/`clear_progress`）。game_cliは単一プロセス・単一asyncioイベントループで完結するため、DB永続化やHTTP通信を使わずシンプルに実装した。
- `app/services/work.py`: `finish_work`を`work_graph.ainvoke(...)`から`work_graph.astream(state, stream_mode=["custom","values"])`に変更し、customイベントを`work_progress`に反映、成功・失敗いずれの終了時も進行状況を破棄するようにした。新規`InProgressWork`データクラス・`list_in_progress_work`・`get_work_session_status`を追加。
- `scripts/game_cli.py`: 作業室「作業内容を確認する」（`view_work_flow`）を、完了済みの成果物一覧に進行中の作業一覧を合わせて新しい順に表示する形に拡張。進行中のものを選ぶと、その時点までの生成内容のスナップショットを担当者ごとに表示する（自動更新はしない一発表示。既存の他の閲覧系フローと同じUXパターンを踏襲）。
- 動作確認: 実際に`game_cli.py`でお題を作成→作業を割り当て→即座に作業室を開き、対象が「（進行中）」として一覧に出ること・選択するとその時点の生成内容（または「まだ生成中です」）が表示されることを確認。完了後は通常の成果物表示に切り替わることも確認。バックエンド側は別途、`work_progress`の内容が時間経過とともに増えていく（一括ではなく逐次届く）ことを直接確認済み。
- 今回の対象外: `graph.py`（1エージェントへのお題実行）のストリーミング化・SSE HTTPエンドポイントの追加・フロントエンドUI実装。`graph.py`はgame_cli.pyから使われておらず現状の目的に対して意味が薄いため対象外にした。SSE化はREST API化という別の未着手ロードマップ項目とセットで行うのが自然なため、次のイテレーションへ送る。

### 追加修正（同日）: 一発スナップショット表示→本当のリアルタイム表示に変更、役割混在バグを修正

最初の実装では、進行中の作業を選ぶと`asyncio.sleep(1)`を挟んだ一発スナップショット表示で終わっており、実質的にリアルタイムには見えていなかった。「timesleep(1)となっていて正常に確認できない」「見るのをやめたい場合Enterでキャンセルできるように」「リアルタイム配信って動いている？」という指摘を受けて修正した。

- `scripts/game_cli.py`に`_watch_work_progress`を新設: `work_progress`を1秒おきにポーリングし、増えた分だけ`print(..., end="", flush=True)`で追記していく、実際に流れて見えるリアルタイム表示にした。作業が完了/失敗すると自動的に監視を終了する。
- Enterキーでの監視中断に対応: `input()`ベースの`_ainput`は一度呼ぶとブロックし安全に中断する手段が無いため、Windows専用の`msvcrt.kbhit()`/`getch()`で非ブロッキングにキー入力を確認する`_enter_pressed`を追加（標準入力がコンソールでない場合は`OSError`を捕捉し「キー検知なし」として扱う。Windows以外の環境では`msvcrt`が無いため常に効かないが、完了時には自動終了するので実害はない）。
- **バグ修正**: 動作確認中に、リーダーがworkerを兼任する場合（社長室で選んだworkerの中からリーダーを選ぶ仕様上、これは特殊ケースではなく通常のケース）、`work_progress`が`agent_id`だけをキーにしていたため、リーダーとしての統合作業のトークンが、同じエージェントのworkerとしての担当パートの続きとして扱われ、表示上「別の担当者のセクションに紛れ込む」形になっていた。`app/services/work_graph.py`が流すイベントに`role`（`"worker"`/`"leader"`）を追加し、`app/services/work_progress.py`のキーを`agent_id`単体から`(agent_id, role)`に変更、`work.py`・`game_cli.py`側もこれに合わせて修正。リーダー分は「（リーダーとして統合中）」という見出しで別枠表示されるようにした。
- 動作確認: workerとリーダーが同一エージェントになる構成で再現テストを行い、修正前は統合作業の内容がworkerの表示に混ざっていたこと、修正後は正しく別セクションとして表示されることを確認した。

## 次のイテレーション（未着手）

- 会議室・雇用・作業・お題のREST API化（フロントエンド結合用）
- WebSocket/SSEによるステップごとのリアルタイムストリーミング配信
- LangGraph checkpointerによる状態のDB永続化（PostgreSQLとの共存設計）
- `skills/` フォルダの実データ化・追加キャラクターのペルソナ整備
- 成果物種別（website/report/proposal）の選択UI・簡易プレビュー表示
