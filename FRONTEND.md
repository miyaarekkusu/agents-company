# FRONTEND.md

`frontend/`（React + Vite + TypeScript）の画面実装タスク一覧です。チームメンバーはこのファイルを見ながら、担当タスクを実装してください。実装前に必ず`AGENTS.md`（ディレクトリ構成・開発ワークフロー・「依頼範囲を超えて変更しない」ルール）も読むこと。

## 実装方針

- **バックエンドとの結合（API・WebSocket接続）は、バックエンド側の実装がある程度固まってから行う。** それまではモックデータ（ダミーのJSON等）を使って、画面の見た目・操作・遷移を作り込む。
- バックエンド側の動作確認は、引き続き`backend-core/scripts/chat.py`等のCLIで行う（フロントエンドの結合を待たずにバックエンド単体の検証ができるようにするため）。フロントエンドの実装はこれと並行して進めてよい。
- 上記の方針のため、以下のタスク一覧は「フェーズA: 画面・操作（結合なし）」「フェーズB: バックエンド結合」に分けている。**まずはフェーズAから着手する。**

## 現状の実装

- `frontend/src/App.tsx`: 社長室・会議室・作業室・メールの実データ結合済み（2026-07-31）。`frontend/src/api/hooks.ts`の`usePolling`で3秒間隔ポーリングし、各RoomにpropsとしてAPIレスポンス・ミューテーション関数を渡す構成。部屋タブをクリックすると確認モーダル→「🚶 ◯◯室へ移動中...」の短い移動演出（`transitioningTo`ステート、0.7秒）→実際の切り替え、という流れになっている。
- `frontend/vite.config.ts`: `/api`を`http://localhost:8000`（backend-core）へプロキシ済み（フェーズBで使用中）。
- 依存関係: `react` `react-dom` のみ。**キャラクター・部屋の見た目はスプライトシート画像ではなく、CSS＋絵文字で表現する方針に確定した**（2026-08）。理由・詳細はAGENTS.mdの「2D部屋シーンの実装方針転換」を参照。新たに画像アセット・アニメーションライブラリを追加する必要はない。
- **キャラクター表示** (`components/Character/CharacterSprite.tsx`): 全キャラクター共通の絵文字（`🧑‍💼`等）だが、`roleId`に応じて役職ごとに異なる絵文字（フロントエンド🧑‍💻・バックエンド🧑‍🔧・フルスタック🧑‍🚀・テックリード🧑‍🏫・PM🧑‍💼・アイデア出し🧑‍🔬・デザイナー🧑‍🎨、社長👑）を割り当てて視覚的な多様性を出している。名前・役職タグを頭上に表示し、状態（`idle`/`walking`/`discussing`/`sitting`/`working`）はCSSアニメーション（`index.css`の`state-*`クラス）と接地シャドウで表現、向き（`front`/`back`/`left`/`right`）は`right`のみ`left`素材をCSSで左右反転して流用する。
- **部屋の家具・当たり判定** (`components/Room/Furniture.tsx`): `FurnitureLayout`（座標・サイズ・`collidable`・`collisionPadding`）と`FurnitureVisual`（CSSのフォールバック表示。`imageUrl`を指定すれば画像表示にも切替可能な設計）を分離した共通の型・コンポーネント。社長室・会議室・作業室で共通して使う。
- **プレイヤー移動** (`components/Room/usePlayerMovement.ts`): WASDキーでの自由移動＋家具との当たり判定（軸ごとに独立判定してスライドできる）を3部屋共通のフックに集約。`seats`（`SeatDefinition[]`）を渡した部屋では、近くの椅子にEキーで着席/起立もできる（社長室の執務椅子、会議室の6脚、作業室のデスクごとの椅子）。`getFurnitureLayout`/`seats`はコンポーネント側で`useCallback`/`useMemo`により参照を固定すること（固定しないと内部の`requestAnimationFrame`ループが毎レンダー再生成され、移動が実質止まって見えるバグの原因になる。実際に発生し修正済み）。
- **会議室の表示ロジック**: エージェントは常時表示ではなく、**実際に会議が進行中のときだけ、その会議の参加者のみ**を椅子に着席させて表示する（`GET /api/meetings/status`が返す`leader_agent_id`/`participant_agent_ids`を使用）。議長は正面向きの一番目立つ席に固定し、頭上に「👑 議長」バッジを表示する。
- **作業室のリアルタイム表示**: 作業中のお題ごとに「🔴 リアルタイムで見る」ボタンがあり、押すと`features/work-progress/WorkProgressModal.tsx`が開く。`GET /api/tasks/{task_id}/work-session`を2秒間隔でポーリングし、経過時間（1秒ごとに再計算）・担当エージェント（リーダーを冠アイコンで明示）・状態（作業中/完了/失敗）を自動更新表示する。完了すると、そのモーダル内にそのまま成果物（`ReportViewer`）を表示する。

## 必要な画面一覧

まずはこの一覧で「何を作るか」の全体像を把握すること。詳細な操作フローは後述のタスク一覧を参照。

| # | 画面/要素 | 概要 |
|---|---|---|
| 1 | 共通シェル・マップ | 社長室・会議室・作業室を行き来する母艦画面。現状の`App.tsx`のルーム切り替えを土台に拡張する。 |
| 2 | キャラクター表示・アニメーション | ✅実装済み（方式変更）。スプライトシートではなくCSS/絵文字ベース。歩行（WASD自由移動、向きに応じた反転）・座る・作業中、の各状態を表現。 |
| 3 | 社長室シーン | ✅実装済み。社長がWASDで部屋の中を動き回れる（執務机・採用デスク・椅子に当たり判定あり、椅子にはEキーで着席可）。お題だし・雇用・報告受け取りの起点。 |
| 4 | 会議室シーン | ✅実装済み。社長もWASDで自由移動・着席可。会議が進行中のときだけ、参加エージェント（議長を明示）がテーブルの椅子に着席して表示される。 |
| 5 | 作業室シーン | ✅実装済み。デスク8台＋椅子を配置。作業中のエージェントは対応する椅子に着席して表示され、「🔴 リアルタイムで見る」ボタンから進捗モーダルを開ける。 |
| 6 | 部屋移動確認モーダル | ✅実装済み。「〇〇部屋に移動しますか？」の確認後、「🚶 移動中...」の短い演出を挟んでから切り替わる。 |
| 7 | お題選択画面 | 会議室の「会議」ボタンから開く。既に出しているお題の中から会議にかけるものを選ぶ。 |
| 8 | リーダー選択画面 | お題選択の次。参加させるエージェントの中からリーダーを1名選ぶ。 |
| 9 | 参加者選択画面 | リーダー選択の次。会議に参加させるエージェントを複数選ぶ。 |
| 10 | 会議終了お知らせ | 会議が終わったタイミングで表示する通知（「会議室までお越しください」）。 |
| 11 | 報告書受け取りモーダル | 会議室でリーダーに近づくと表示される、報告書を受け取るかの確認モーダル。 |
| 12 | 報告書 / 作業内容確認画面 | 報告書受け取りモーダルの先、および作業室での成果物確認から共通で開く画面（同一コンポーネント）。 |
| 13 | エージェント雇用画面 | ボタンから開く。キャラクター選択（性格・スキル・役割を表示）＋AIモデル選択（説明付き）。 |
| 14 | 全エージェント作業内容表示画面 | ✅実装済み（方式変更）。WebSocketではなく、作業中のお題ごとに開くモーダル（`WorkProgressModal`）が2秒間隔のポーリングで進捗を自動更新表示する。 |
| 15 | エージェントメッセージ受信通知 | エージェントから確認・質問のメッセージが来たことを知らせる通知。 |
| 16 | エージェントとの対話（チャット）画面 | 携帯電話のメッセージアプリのようなUIで、エージェントとやり取りする画面。 |

## 画面ごとの補足

- **#12（報告書 / 作業内容確認画面）**は、会議レポート（`DATABASE.md`の`meeting_reports`）と、作業成果物（同`artifacts`: website/report/proposal）の**両方の表示に使う共通コンポーネント**として設計する。中身の種別（テキストのレポートか、Webサイトの簡易プレビューか）で表示を出し分ける。
- **#14（全エージェント作業内容表示）**は、当初はWebSocketでの全エージェント一括ストリーミングを想定していたが、実装では**お題ごとにモーダルを開き、`GET /api/tasks/{task_id}/work-session`をポーリングする方式**に変更した（`features/work-progress/WorkProgressModal.tsx`）。トークン単位のリアルタイムストリーミングは引き続き未実装（ロードマップ参照）で、ここでの「リアルタイム」は数秒おきの自動更新を指す。
- **#16（対話画面）**は、`DATABASE.md`の`email_threads`/`email_messages`（バックエンド上は「メール」として設計）を、見た目上はチャットアプリ風に表示するもの。データモデルと見た目の対応関係をコメント等で明記しておくこと。
- **#7〜#9（お題選択・リーダー選択・参加者選択）**は、`DATABASE.md`の`tasks`/`agents`/`meetings`/`meeting_participants`に対応する。

## 実装タスク一覧

進捗はここでチェックし、完了したら`AGENTS.md`の「今後の実装予定（ロードマップ）」→「フロントエンド」節にも反映すること。

### フェーズA: 画面・操作の実装（バックエンド結合なし、モックデータで動作）

> 実際には、モックのみで進める独立フェーズは設けず、フェーズBのバックエンド結合と一体で実装が進んだ（下記フェーズC参照）。以下は元の計画時点のタスク分解として残す。

- [x] スプライトアニメーションの実装方式を検討・決定する（CSS / Canvas / ライブラリ）→ **CSS＋絵文字方式に決定**（2026-08。画像スプライトシートは不採用。理由はAGENTS.md参照）
- [x] キャラクター表示コンポーネント: 歩行（WASD自由移動、向きに応じた反転）・座る・作業中の各状態を実装する（#2。「話し合う」は現状「座る」状態の流用で表現）
- [x] 社長室・会議室・作業室、各シーンの背景・レイアウトを実装する（#3〜5。キャラクターが部屋の中を動き回れる。CSSのfloor-*クラス＋家具はCSSグラデーション/絵文字ボックス）
- [x] ドア接触→部屋移動の確認モーダル→遷移、を実装する（#6。確認モーダルの後に短い移動演出を追加済み）
- [ ] お題選択画面（モックのお題一覧から選択）を実装する（#7）
- [ ] リーダー選択画面（モックのエージェント一覧から1名選択）を実装する（#8）
- [ ] 参加者選択画面（モックのエージェント一覧から複数選択）を実装する（#9）
- [ ] 会議終了お知らせ（トースト/通知UI）を実装する（#10。表示条件はモックのタイマー等で仮実装してよい）
- [ ] リーダーキャラクターが会議室に待機する表示と、プレイヤーが近づいた際の報告書受け取りモーダルを実装する（#11）
- [ ] 報告書 / 作業内容確認画面（共通コンポーネント）を実装する。モックのレポート文・モックのWebサイトHTMLなど複数種別で表示確認する（#12）
- [ ] エージェント雇用画面を実装する: キャラクター選択（性格・スキル・役割の表示）＋AIモデル選択（モデルごとの説明を表示）。モックのエージェント候補・モックのAIモデル一覧を使う（#13）
- [x] 全エージェント作業内容表示画面のレイアウトを実装する（#14。最終的にはお題ごとのモーダル＋ポーリング方式で実装。フェーズC参照）
- [ ] エージェントメッセージ受信通知UIを実装する（#15。モックの通知を表示するだけでよい）
- [ ] エージェントとの対話（チャット）画面を実装する: 携帯アプリ風のUI、モックの会話ログを表示する（#16）

### フェーズB: バックエンド結合（2026-07-31 完了）

- [x] APIクライアントの土台を作成する（`src/api/{types,client,hooks}.ts`。型はバックエンドの`app/schemas/`と一致させる）
- [x] エージェント雇用画面を実データに接続する（`roles`/`skills`/`ai-models`のAPI。役職変更時に`GET /api/ai-models?role_id=`を再取得し、上流工程役職でのAIモデル選択のハード制限はサーバー側の絞り込み結果をそのまま使う）
- [x] お題選択・リーダー選択・参加者選択画面を実データに接続する（`tasks`/`agents`/`meetings`関連API。busyなエージェントは`GET /api/agents/busy`で選択肢から除外）
- [x] 報告書 / 作業内容確認画面を実データに接続する（`meeting_reports`相当の`PendingReportOut`/`artifacts`API）
- [x] 進行中の作業・会議の表示を実データに接続する（#14相当。ただしWebSocketではなく**3秒間隔のポーリング**（`usePolling`）に方針変更。トークン単位のリアルタイムストリーミング表示は今回のスコープ外のまま）
- [x] メール（`email_threads`/`email_messages`）を実データに接続する。ただし**返信機能はバックエンド未実装のため、既読/未読の通知一覧（読み取り専用）に変更**（旧モックのクイック返信ボタン・自動成果物生成演出は撤去）
- [x] 会議室使用中・作業室使用中の表示を実データに接続する（`GET /api/meetings/status`・`GET /api/work/status`のポーリング）

**フェーズBで発生した、モックUIからの意図的な仕様変更**（実際のバックエンド機能・`scripts/game_cli.py`の画面構成に合わせるための調整）:
- 作業の割り当て（worker/leader選択）は**社長室**に統一（`game_cli.py`の「作業を割り当てる」も社長室メニューにあるため）。作業室は成果物閲覧＋進行中作業の表示のみを担う。
- 雇用画面の固定候補カード（`MOCK_CANDIDATES`）は、名前・性格欄のプリフィル用「テンプレート」としてのみ残し、送信は自由入力＋実カタログでの`POST /api/agents`呼び出しに変更。
- キャラクターの歩行演出（`triggerAgentWalk`）は撤去し、`agent.id`から算出する静止レイアウトに簡略化（スプライト/画像アニメーション自体は今回のスコープ外）。

### フェーズC: 2D部屋シーンの作り込み・リアルタイム表示（2026-08 完了）

フェーズBでは各Roomのレイアウトは静的な配置のみだったが、本フェーズで実際に「部屋の中を移動する」「椅子に座る」「進行状況をリアルタイムに見る」体験を実装した。

- [x] キャラクター表示を画像スプライトシートからCSS＋絵文字ベースに全面刷新（`components/Character/CharacterSprite.tsx`）。役職ごとに異なる絵文字を割り当てて視覚的な多様性を確保。
- [x] 部屋の背景・家具をCSSグラデーション＋絵文字ベースの表現に統一（`components/Room/Furniture.tsx`の`FurnitureLayout`/`FurnitureVisual`に一本化）。かつて試した`room.png`等の背景画像・抽出スプライト画像は不採用に。
- [x] 社長（プレイヤー）がWASDキーで社長室・会議室・作業室のいずれでも自由に移動できるようにする（`components/Room/usePlayerMovement.ts`という3部屋共通のフックに集約）。家具には当たり判定（`collisionPadding`で見た目の箱より少し広めに）があり、めり込まない。
- [x] 椅子にEキーで近づいて着席/起立できるようにする（`SeatDefinition`。社長室の執務椅子・会議室の6脚・作業室のデスクごとの椅子）。
- [x] 会議室: エージェントは会議進行中のときだけ、その会議の参加者のみを椅子に着席させて表示（`GET /api/meetings/status`拡張の`leader_agent_id`/`participant_agent_ids`を使用）。議長を「👑 議長」バッジで明示。
- [x] 部屋移動時に「🚶 ◯◯室へ移動中...」の短い演出を挟む（`App.tsx`の`transitioningTo`ステート）。
- [x] 作業室に「🔴 リアルタイムで見る」ボタン＋進捗モーダル（`features/work-progress/WorkProgressModal.tsx`）を追加。`GET /api/tasks/{task_id}/work-session`を2秒間隔でポーリングし、経過時間・担当エージェント・状態を自動更新、完了時は成果物をその場に表示。

**実装上の既知の落とし穴（同種のバグを再発させないための記録）**:
- `usePlayerMovement`に渡す`getFurnitureLayout`/`seats`は、呼び出し側コンポーネントで`useCallback`/`useMemo`により参照を安定させること。安定させないと、内部の`requestAnimationFrame`ループが依存配列の変化で毎レンダー再生成され続け、移動がほぼ止まって見えるバグになる（実際に発生し修正済み）。
- 椅子のような「座席として使う家具」に当たり判定（`collidable: true`）を持たせると、座席アンカーが当たり判定の内側にあるため、立ち上がった瞬間に自分の位置がすでに障害物と重なり、二度と動けなくなるバグになる（実際に発生し修正済み）。座席として使う家具は`collidable: false`にすること。

## ディレクトリ構成（現状）

```
frontend/src/
├── api/                    # fetchクライアント・レスポンス型・usePollingフック（types.ts / client.ts / hooks.ts）
├── mocks/                  # フェーズAの名残（現在は実データ接続済みのため未使用）
├── components/
│   ├── Common/              # Modal, Toast 等の汎用UIパーツ
│   ├── Character/           # CharacterSprite.tsx（役職別絵文字ベースのキャラクター表示）
│   └── Room/                 # Furniture.tsx（家具レイアウト・当たり判定の共通型）、usePlayerMovement.ts（WASD移動＋着席の共通フック）
├── rooms/
│   ├── President/            # 社長室シーン
│   ├── Meeting/               # 会議室シーン（お題/リーダー/参加者選択、会議進行中の参加者表示を含む）
│   └── Work/                  # 作業室シーン（進行中お題の一覧・リアルタイム進捗ボタンを含む）
├── features/
│   ├── hiring/                 # エージェント雇用画面
│   ├── report-viewer/          # 報告書 / 作業内容確認画面（共通コンポーネント）
│   ├── mobile-chat/            # エージェントからの通知をチャット風UIで表示
│   └── work-progress/          # 作業状況リアルタイム表示モーダル（WorkProgressModal.tsx）
├── image/                   # 過去に試した画像ベースの部屋・キャラクター素材（現在は不採用のため未使用。削除しても実害はないが判断は別途）
├── App.tsx
├── main.tsx
└── index.css
```

## 実装時の注意

- `AGENTS.md`の「開発ワークフロー（タスクとエージェントの分割）」に従い、画面・機能ごとに担当を分けて実装すること。担当範囲外のファイルには触れない。
- フェーズAはモックデータで進めるため、バックエンドの実装状況を待たずに複数人で並行着手できる。
- APIレスポンスの正は`backend-core/app/schemas/`配下の実装。`DATABASE.md`はDBスキーマ（将来像）であり、現時点のAPIレスポンス形式そのものではない。
- コミットメッセージ・コード内コメントは日本語で統一する。
- UIの見た目（配色・キャラクター表現等）はAGENTS.mdの「キャラクター例」「拠点構成」を参考にする。

## 関連ドキュメント

- `AGENTS.md`: プロジェクト全体のガイド・アーキテクチャ方針・開発ワークフロー。
- `DATABASE.md`: DBスキーマ設計（フェーズB以降のAPIが依拠する構造）。
- `memo/変更履歴.md`: 実装が完了した際の記録先。
